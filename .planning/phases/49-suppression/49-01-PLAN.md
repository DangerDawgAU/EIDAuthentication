---
phase: 49-suppression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EIDCredentialProvider/Dll.cpp
  - EIDCredentialProvider/CEIDProvider.cpp
  - EIDCredentialProvider/CEIDCredential.cpp
  - EIDCredentialProvider/helpers.cpp
  - EIDCardLibrary/CredentialManagement.cpp
  - EIDCardLibrary/CContainerHolderFactory.cpp
  - EIDCardLibrary/CContainer.cpp
  - EIDCardLibrary/CSmartCardNotifier.cpp
  - EIDCardLibrary/CertificateUtilities.cpp
  - EIDCardLibrary/Tracing.cpp
  - EIDConfigurationWizard/EIDConfigurationWizardPage04.cpp
  - EIDConfigurationWizard/EIDConfigurationWizardPage05.cpp
  - EIDConfigurationWizard/EIDConfigurationWizardPage06.cpp
  - EIDConfigurationWizard/EIDConfigurationWizardPage07.cpp
  - EIDConfigurationWizard/CContainerHolder.cpp
  - EIDLogManager/EIDLogManager.cpp
  - EIDAuthenticationPackage/EIDSecuritySupportProvider.cpp
  - .planning/phases/49-suppression/49-VERIFICATION.md
autonomous: true
requirements:
  - SUPPR-01
  - SUPPR-02
  - SUPPR-03
  - SUPPR-04

must_haves:
  truths:
    - "All Windows API compatibility issues marked with NOSONAR and rationale"
    - "All LSASS-safety issues (std::string, dynamic allocation) marked with NOSONAR and rationale"
    - "All COM/SEH constraint issues marked with NOSONAR and rationale"
    - "Every NOSONAR has an inline comment explaining why the issue cannot be fixed"
  artifacts:
    - path: "EIDCredentialProvider/Dll.cpp"
      provides: "Credential Provider DLL entry points"
      contains: "void** ppv"
    - path: "EIDCardLibrary/CredentialManagement.cpp"
      provides: "Credential and context management"
      contains: "new CCredential"
    - path: "EIDCardLibrary/Tracing.cpp"
      provides: "Tracing and audit functions"
      contains: "..."
    - path: ".planning/phases/49-suppression/49-VERIFICATION.md"
      provides: "Documentation of all suppressions"
  key_links:
    - from: "All .cpp files"
      to: "SonarQube analysis"
      via: "NOSONAR inline comments"
      pattern: "// NOSONAR - <CODE>: <rationale>"
---
<objective>
Add NOSONAR suppressions with documented rationale to all remaining unavoidable SonarQube issues across the codebase, ensuring every suppression has an inline comment explaining why the issue cannot be fixed.

Purpose: Achieve zero registered SonarQube issues by properly suppressing all issues that are technically unavoidable due to Windows API constraints, LSASS safety requirements, or COM/SEH patterns.

Output: All source files with remaining issues have proper NOSONAR suppressions with rationale codes, and VERIFICATION.md documenting all suppressions by category.
</objective>

<execution_context>
@C:/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-critical-fixes/45-01-SUMMARY.md

# Phase 49 Requirements (from ROADMAP)
- SUPPR-01: Windows API compatibility issues suppressed with rationale
- SUPPR-02: LSASS-safety issues suppressed with rationale
- SUPPR-03: COM/SEH constraint issues suppressed with rationale
- SUPPR-04: Every NOSONAR has inline rationale comment

# Key Constraints
- **LSASS context:** No exceptions, no console I/O, static CRT required
- **Windows 7 support:** Must use v143 toolset
- **C++23 flag:** Use `/std:c++23preview`
- **Suppression format:** `// NOSONAR - <CODE>: <rationale>` (must be on same line as issue)

# Rationale Codes
| Code | Description |
|------|-------------|
| RUNTIME-01 | Value set/modified at runtime by Windows or LSA |
| GLOBAL-01 | Non-const for Windows API compatibility |
| HANDLE-01 | Windows handle type (ULONG_PTR), not pointer |
| RESOURCE-01 | RC.exe requires #define macros |
| MACRO-02 | Preprocessor macro required for __FILE__/__LINE__ or SDK compatibility |
| SEH-01 | Structured exception handling pattern required |
| LSASS-01 | LSASS memory safety constraint |
| ENUM-01 | Unscoped enum required for Windows SDK compatibility |
| CONTROL-01 | Empty block for documented reason |
| EXPLICIT-TYPE-01/02/03 | Explicit type for security audit visibility |
| COM-01 | COM object creation requires new/delete |
| CAST-01 | C-style cast required for Windows API compatibility |
| VARIADIC-01 | Variadic function required for logging/tracing |

# Remaining Issue Categories (from Phase 45 baseline - 722 issues)
Based on current analysis:
1. **void* usage** (~15 occurrences) - Windows API requirement (SUPPR-01)
2. **new/delete usage** (~13 occurrences) - COM object creation (SUPPR-02)
3. **C-style casts removing const** (~10 occurrences) - Windows API (SUPPR-01)
4. **ellipsis notation** (~4 occurrences) - variadic functions (SUPPR-02)
5. **const_cast usage** (~4 occurrences) - COM locking pattern (SUPPR-03)

# Current State
Many suppressions already exist. Check existing patterns:
- EIDConfigurationWizard/*.cpp - Many NOSONAR comments present
- EIDLogManager/*.cpp - Some NOSONAR comments present
- EIDCredentialProvider/*.cpp - Needs review for missing suppressions
- EIDCardLibrary/*.cpp - Needs review for missing suppressions
- EIDAuthenticationPackage/*.cpp - Needs review for missing suppressions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suppress void* and C-style cast issues (SUPPR-01)</name>
  <files>EIDCredentialProvider/Dll.cpp, EIDCredentialProvider/CEIDProvider.cpp, EIDCardLibrary/CSmartCardNotifier.cpp, EIDCardLibrary/CertificateUtilities.cpp, EIDConfigurationWizard/CContainerHolder.cpp</files>
  <action>
Add NOSONAR suppressions to void* usage and C-style cast issues that are required for Windows API compatibility.

**void* pattern:** Windows APIs like QueryInterface, CreateInstance, and COM interfaces require void** parameters. These cannot be changed.

**C-style cast pattern:** Windows APIs often require casting between related types (e.g., LPTSTR* to LPTSTR, HANDLE to specific types). These casts are safe and required.

**For each file, find and add suppressions:**

1. **EIDCredentialProvider/Dll.cpp:**
   - Lines with `void** ppv` parameters (QueryInterface, CreateInstance, DllGetClassObject)
   - Format: `void** ppv)  // NOSONAR - CAST-01: COM interface requires void** for QueryInterface`

2. **EIDCredentialProvider/CEIDProvider.cpp:**
   - Lines with `reinterpret_cast<void**>` or `void**` usage
   - Format: `// NOSONAR - CAST-01: COM QueryInterface requires void**`

3. **EIDCardLibrary/CSmartCardNotifier.cpp:**
   - Lines with `(LPVOID)`, `(LPWSTR)`, `(LPTSTR)` casts for Windows smart card APIs
   - Format: `// NOSONAR - CAST-01: Windows SCard API requires non-const pointer`

4. **EIDCardLibrary/CertificateUtilities.cpp:**
   - Lines with `(LPBYTE)`, `(LPTSTR)`, `(LPWSTR)` casts for certificate APIs
   - Format: `// NOSONAR - CAST-01: Windows Crypto API requires non-const buffer pointer`

5. **EIDConfigurationWizard/CContainerHolder.cpp:**
   - Lines with `(PTSTR)` casts for memory allocator
   - Format: `// NOSONAR - CAST-01: EIDAlloc returns void*, cast required for type safety`

**Implementation notes:**
- NOSONAR must be on the SAME LINE as the issue
- Use proper rationale code (CAST-01 for Windows API casts)
- Keep comment concise but explanatory
  </action>
  <verify>
1. Build passes: `powershell -File build.ps1`
2. Grep shows suppressions added:
   ```bash
   grep -n "void\*\* ppv" EIDCredentialProvider/Dll.cpp
   grep -n "CAST-01" EIDCardLibrary/CSmartCardNotifier.cpp
   ```
3. Count of void* lines without NOSONAR should be zero for target files
  </verify>
  <done>
- All void* parameters in COM interfaces have NOSONAR with CAST-01 rationale
- All C-style casts for Windows APIs have NOSONAR with CAST-01 rationale
- Build passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Suppress new/delete and dynamic allocation issues (SUPPR-02)</name>
  <files>EIDCredentialProvider/Dll.cpp, EIDCredentialProvider/CEIDProvider.cpp, EIDCredentialProvider/CEIDCredential.cpp, EIDCardLibrary/CredentialManagement.cpp, EIDCardLibrary/CContainerHolderFactory.cpp, EIDConfigurationWizard/EIDConfigurationWizardPage04.cpp, EIDConfigurationWizard/CContainerHolder.cpp</files>
  <action>
Add NOSONAR suppressions to new/delete usage that is required for COM object creation and credential management.

**new/delete pattern:** COM objects and credential structures must be heap-allocated. The credential provider framework requires dynamic allocation. While LSASS-safe code should avoid dynamic allocation, the credential provider and configuration wizard run in user mode where it is acceptable.

**For each file, find and add suppressions:**

1. **EIDCredentialProvider/Dll.cpp:**
   - `new CClassFactory` - COM class factory must be heap-allocated
   - `delete this` - COM self-destruction pattern
   - Format: `// NOSONAR - COM-01: COM class factory requires heap allocation`

2. **EIDCredentialProvider/CEIDProvider.cpp:**
   - `new CMessageCredential()` - Credential object allocation
   - `new CSmartCardConnectionNotifier()` - Notifier allocation
   - `new CEIDProvider()` - Provider allocation
   - Format: `// NOSONAR - COM-01: Credential Provider framework requires heap allocation`

3. **EIDCredentialProvider/CEIDCredential.cpp:**
   - `delete _pContainer` - Container cleanup
   - Format: `// NOSONAR - COM-01: COM object cleanup pattern`

4. **EIDCardLibrary/CredentialManagement.cpp:**
   - `new CCredential()` - Credential allocation
   - `new WCHAR[]` - PIN buffer allocation
   - `new CSecurityContext()` - Context allocation
   - `new CUsermodeContext()` - User mode context allocation
   - `delete toDelete` - Context cleanup
   - Format: `// NOSONAR - COM-01: Credential lifecycle requires heap allocation`

5. **EIDCardLibrary/CContainerHolderFactory.cpp:**
   - `new CContainer()` - Container allocation
   - `new T()` - Template holder allocation
   - Format: `// NOSONAR - COM-01: Container factory requires heap allocation`

6. **EIDConfigurationWizard/EIDConfigurationWizardPage04.cpp:**
   - `new CContainerHolderFactory<CContainerHolderTest>()` - UI list allocation
   - `delete pCredentialList` - List cleanup
   - Format: `// NOSONAR - COM-01: UI credential list requires heap allocation`

7. **EIDConfigurationWizard/CContainerHolder.cpp:**
   - `delete _pContainer` - Container cleanup
   - `delete this` - Self-destruction pattern
   - Format: `// NOSONAR - COM-01: COM cleanup pattern`

**Implementation notes:**
- NOSONAR must be on the SAME LINE as the new/delete
- Use COM-01 rationale code for all COM-related allocations
- For EIDCardLibrary in LSASS context, use LSASS-01 if truly in LSASS
  </action>
  <verify>
1. Build passes: `powershell -File build.ps1`
2. Grep shows suppressions added:
   ```bash
   grep -n "new.*CClassFactory" EIDCredentialProvider/Dll.cpp
   grep -n "COM-01" EIDCardLibrary/CredentialManagement.cpp
   ```
3. Count of new/delete lines without NOSONAR in target files should be zero
  </verify>
  <done>
- All new/delete usage in COM objects have NOSONAR with COM-01 rationale
- All dynamic allocations in user-mode components have NOSONAR
- Build passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Suppress variadic function and const_cast issues (SUPPR-02, SUPPR-03)</name>
  <files>EIDCardLibrary/Tracing.cpp, EIDCardLibrary/CContainerHolderFactory.cpp, EIDCardLibrary/StringConversion.cpp, EIDLogManager/EIDLogManager.cpp</files>
  <action>
Add NOSONAR suppressions to variadic functions (required for tracing/logging) and const_cast usage (required for COM locking patterns).

**Variadic function pattern:** Tracing and logging functions must accept variable arguments. C-style ellipsis is the standard way to achieve this in Windows code.

**const_cast pattern:** COM locking patterns (AddRef/Release) require const_cast when calling non-const methods from const methods.

**For each file, find and add suppressions:**

1. **EIDCardLibrary/Tracing.cpp:**
   - `EIDCardLibraryTraceEx(...)` - Variadic tracing function
   - `EIDSecurityAuditEx(...)` - Variadic audit function
   - Format: `// NOSONAR - VARIADIC-01: ETW tracing requires variadic format function`

2. **EIDCardLibrary/StringConversion.cpp:**
   - `std::wstring Format(const wchar_t* format, ...)` - Variadic format function
   - Format: `// NOSONAR - VARIADIC-01: Format function requires variadic arguments`

3. **EIDCardLibrary/CContainerHolderFactory.cpp:**
   - `const_cast<CContainerHolderFactory<T>*>(this)->Lock()` - COM locking from const method
   - `const_cast<CContainerHolderFactory<T>*>(this)->Unlock()` - COM unlocking
   - Format: `// NOSONAR - COM-01: AddRef/Release pattern requires const_cast for thread-safe locking`

4. **EIDLogManager/EIDLogManager.cpp:**
   - Any variadic or const_cast usage (verify and add if present)

**Implementation notes:**
- NOSONAR must be on the SAME LINE as the issue
- Use VARIADIC-01 for ellipsis notation
- Use COM-01 for const_cast in COM locking patterns
  </action>
  <verify>
1. Build passes: `powershell -File build.ps1`
2. Grep shows suppressions added:
   ```bash
   grep -n "VARIADIC-01" EIDCardLibrary/Tracing.cpp
   grep -n "const_cast.*Lock" EIDCardLibrary/CContainerHolderFactory.cpp
   ```
3. All variadic function declarations have NOSONAR
4. All const_cast usage has NOSONAR
  </verify>
  <done>
- All variadic functions have NOSONAR with VARIADIC-01 rationale
- All const_cast usage has NOSONAR with COM-01 rationale
- Build passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 4: Suppress remaining issues and verify all suppressions have rationale (SUPPR-04)</name>
  <files>Multiple .cpp files, .planning/phases/49-suppression/49-VERIFICATION.md</files>
  <action>
Perform comprehensive scan for any remaining issues without NOSONAR suppressions, ensure all existing NOSONAR comments have proper rationale format, and create verification documentation.

**Step 1: Find remaining issues without suppressions**
Search for common patterns that should have NOSONAR:
```bash
# Find void** without NOSONAR
grep -rn "void\*\*" --include="*.cpp" | grep -v NOSONAR

# Find new without NOSONAR
grep -rn "= new " --include="*.cpp" | grep -v NOSONAR | grep -v "// .*new"

# Find delete without NOSONAR
grep -rn "delete " --include="*.cpp" | grep -v NOSONAR | grep -v "// .*delete"

# Find variadic functions without NOSONAR
grep -rn "\.\.\.\)" --include="*.cpp" | grep -v NOSONAR

# Find const_cast without NOSONAR
grep -rn "const_cast" --include="*.cpp" | grep -v NOSONAR
```

**Step 2: Verify all existing NOSONAR comments have rationale**
```bash
# Find NOSONAR without proper format (should be // NOSONAR - CODE:)
grep -rn "NOSONAR" --include="*.cpp" | grep -v "NOSONAR -"
```

**Step 3: Add missing suppressions**
For each issue found without NOSONAR:
1. Determine the appropriate rationale code
2. Add inline suppression on the same line
3. Ensure comment explains WHY the issue cannot be fixed

**Step 4: Create VERIFICATION.md**
Document all suppressions organized by category:
```markdown
# Phase 49: Suppression Verification

## Summary
- Total suppressions added: N
- Files modified: N
- Categories: Windows API (CAST-01), COM (COM-01), Variadic (VARIADIC-01), etc.

## SUPPR-01: Windows API Compatibility
| File | Line | Issue | Rationale |
|------|------|-------|-----------|
| Dll.cpp | 60 | void** ppv | CAST-01: COM QueryInterface |

## SUPPR-02: LSASS-Safety Issues
| File | Line | Issue | Rationale |
|------|------|-------|-----------|
| CredentialManagement.cpp | 51 | new CCredential | COM-01: Credential lifecycle |

## SUPPR-03: COM/SEH Constraints
| File | Line | Issue | Rationale |
|------|------|-------|-----------|
| CContainerHolderFactory.cpp | 427 | const_cast | COM-01: AddRef/Release pattern |

## SUPPR-04: Rationale Coverage
All NOSONAR comments include:
- Rationale code (e.g., CAST-01, COM-01, VARIADIC-01)
- Brief explanation of why issue cannot be fixed

## Build Verification
- Build status: PASSED
- Errors: 0
- Artifacts: All 7 DLLs/EXEs present
```
  </action>
  <verify>
1. Build passes: `powershell -File build.ps1`
2. No lines with void**, new, delete, ..., const_cast lacking NOSONAR (where appropriate)
3. All NOSONAR comments follow format: `// NOSONAR - <CODE>: <rationale>`
4. VERIFICATION.md created with complete documentation
5. Git status shows expected file changes
  </verify>
  <done>
- All remaining SonarQube issues have NOSONAR suppressions
- Every NOSONAR has proper rationale code and explanation
- Build passes with zero errors
- VERIFICATION.md documents all suppressions by category
- Requirements SUPPR-01, SUPPR-02, SUPPR-03, SUPPR-04 satisfied
  </done>
</task>

</tasks>

<verification>
## Overall Phase Verification

1. **SUPPR-01 Verification**: Windows API compatibility issues suppressed
   - All void** parameters have CAST-01 suppressions
   - All C-style casts for Windows APIs have CAST-01 suppressions

2. **SUPPR-02 Verification**: LSASS-safety issues suppressed
   - All new/delete in user-mode have COM-01 suppressions
   - All variadic functions have VARIADIC-01 suppressions

3. **SUPPR-03 Verification**: COM/SEH constraints suppressed
   - All const_cast in COM patterns have COM-01 suppressions
   - SEH-related issues documented (if any remain)

4. **SUPPR-04 Verification**: Every NOSONAR has rationale
   - All NOSONAR comments follow format: `// NOSONAR - <CODE>: <explanation>`
   - No bare NOSONAR comments without rationale

5. **Build Verification**:
   - All 7 projects compile with 0 errors
   - No new warnings introduced
   - All artifacts present in x64/Release/

## Commands
```powershell
# Full build
powershell -File build.ps1

# Verify artifacts
dir x64\Release\*.dll, x64\Release\*.exe, x64\Release\*.lib

# Count suppressions
grep -r "NOSONAR" --include="*.cpp" | wc -l
```
</verification>

<success_criteria>
- [ ] All Windows API compatibility issues marked with NOSONAR and rationale (SUPPR-01)
- [ ] All LSASS-safety issues (std::string, dynamic allocation) marked with NOSONAR and rationale (SUPPR-02)
- [ ] All COM/SEH constraint issues marked with NOSONAR and rationale (SUPPR-03)
- [ ] Every NOSONAR has an inline comment explaining why the issue cannot be fixed (SUPPR-04)
- [ ] Build passes with zero errors
- [ ] VERIFICATION.md created documenting all suppressions by category
</success_criteria>

<output>
After completion, create `.planning/phases/49-suppression/49-01-SUMMARY.md`
</output>
