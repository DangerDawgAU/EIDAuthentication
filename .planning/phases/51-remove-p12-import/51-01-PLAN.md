---
phase: 51-remove-p12-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EIDConfigurationWizard/EIDConfigurationWizard.rc
  - EIDConfigurationWizard/EIDConfigurationWizardPage03.cpp
autonomous: true
requirements:
  - UIUX-01
user_setup: []

must_haves:
  truths:
    - "User cannot see P12 import controls in the Configure Smart Card wizard"
    - "User cannot trigger P12 import functionality through any UI element"
    - "Build compiles without errors after P12 removal"
  artifacts:
    - path: "EIDConfigurationWizard/EIDConfigurationWizard.rc"
      provides: "Dialog resource definitions"
      excludes: "IDC_03IMPORT, IDC_03FILENAME, IDC_03SELECTFILE, IDC_03IMPORTPASSWORD, IDC_STC2, IDC_STC3 controls"
    - path: "EIDConfigurationWizard/EIDConfigurationWizardPage03.cpp"
      provides: "Page 03 dialog procedure"
      excludes: "HandleImportOption function, IDC_03IMPORT handling"
  key_links:
    - from: "WndProc_03NEW PSN_WIZNEXT handler"
      to: "Option handlers"
      via: "IsDlgButtonChecked checks"
      pattern: "IsDlgButtonChecked.*IDC_03IMPORT"
---

<objective>
Remove the legacy P12 import option from the Configuration Wizard to provide a cleaner, more focused user experience.

Purpose: The P12 import feature is rarely used for the target use case (local account smart card authentication on non-domain machines). Removing it simplifies the UI and reduces user confusion.

Output: Cleaner Configuration Wizard with only the "Create new certification authority" and "Use this certification authority" options visible.
</objective>

<execution_context>
@C:/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md

# Current Implementation

**Resource File (EIDConfigurationWizard.rc):**
Lines 139-152 define the P12 import UI controls in IDD_03NEW dialog:
- IDC_03IMPORT (radio button) - "Import a p12 file into the smart card"
- IDC_03FILENAME (edit text) - File path input
- IDC_03SELECTFILE (push button) - "..." file browser button
- IDC_03IMPORTPASSWORD (edit text) - Password input
- IDC_STC2 (static text) - "Password:" label
- IDC_STC3 (static text) - "File:" label

**Source File (EIDConfigurationWizardPage03.cpp):**
- Line 16: Forward declaration for `HandleImportOption`
- Lines 271-283: `HandleImportOption()` function implementation
- Line 191: `CheckDlgButton(hWnd, IDC_03IMPORT, BST_UNCHECKED)` in UpdateCertificatePanel
- Lines 85-86: `SelectFile()` sets IDC_03FILENAME and checks IDC_03IMPORT
- Line 338: `IsDlgButtonChecked(hWnd, IDC_03IMPORT) && HandleImportOption(hWnd)` in PSN_WIZNEXT handler
- Line 396-398: `IDC_03SELECTFILE` command handler

**NOT TO BE REMOVED:**
- `ImportFileToSmartCard()` in EIDCardLibrary/CertificateUtilities.cpp - This function may be used elsewhere and is not part of the UI removal scope.
- Resource ID definitions in EIDConfigurationWizard.h - Keep these to maintain resource ID stability (they just won't be used).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove P12 import controls from dialog resource</name>
  <files>EIDConfigurationWizard/EIDConfigurationWizard.rc</files>
  <action>
    Remove the P12 import UI controls from the IDD_03NEW dialog (approximately lines 139-152).

    **Controls to REMOVE from IDD_03NEW:**
    1. Line 139-140: IDC_03IMPORT radio button - "&Import a p12 file into the smart card"
    2. Line 148: IDC_03IMPORTPASSWORD edit text (password field)
    3. Line 149: IDC_STC2 static text - "Password :"
    4. Line 150: IDC_STC3 static text - "File :"
    5. Line 151: IDC_03FILENAME edit text (file path field)
    6. Line 152: IDC_03SELECTFILE push button "..."

    **Preserve these controls:**
    - All other controls in IDD_03NEW (IDC_03_CREATE, IDC_03USETHIS, IDC_03CERTIFICATEPANEL, IDC_03SHOW, IDC_03SELECT, IDC_03VALIDITYYEARS, IDC_03VALIDITYLABEL, IDC_03VALIDITYWARNING, IDC_03DELETE)

    **Dialog layout note:**
    After removal, the dialog will be cleaner with only:
    - "Create new certification authority" option
    - "Use this certification authority" option
    - Certificate panel and buttons
    - Validity years control
    - Delete checkbox

    The dialog height (220) and positions of remaining controls do not need adjustment since the import controls were at the bottom.
  </action>
  <verify>
    1. Open EIDConfigurationWizard.rc and confirm no P12 import controls remain in IDD_03NEW
    2. Verify IDC_03IMPORT, IDC_03FILENAME, IDC_03SELECTFILE, IDC_03IMPORTPASSWORD, IDC_STC2, IDC_STC3 are not referenced in IDD_03NEW dialog definition
  </verify>
  <done>
    IDD_03NEW dialog definition contains no P12 import related controls. Only Create, UseThis, Certificate Panel, Validity, and Delete controls remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove P12 import handling code from Page03</name>
  <files>EIDConfigurationWizard/EIDConfigurationWizardPage03.cpp</files>
  <action>
    Remove all P12 import related code from EIDConfigurationWizardPage03.cpp.

    **Code sections to REMOVE:**

    1. **Line 16: Forward declaration**
       Remove: `static BOOL HandleImportOption(HWND hWnd);`

    2. **Lines 271-283: HandleImportOption function**
       Remove the entire function:
       ```cpp
       static BOOL HandleImportOption(HWND hWnd)
       {
           EIDCardLibraryTrace(WINEVENT_LEVEL_WARNING, L"IDC_03IMPORT");
           std::wstring szFileName = EID::GetWindowTextW(GetDlgItem(hWnd, IDC_03FILENAME));
           std::wstring wszImportPassword = EID::GetWindowTextW(GetDlgItem(hWnd, IDC_03IMPORTPASSWORD));
           if (!ImportFileToSmartCard((PWSTR)szFileName.c_str(), (PWSTR)wszImportPassword.c_str(), szReader, szCard))
           {
               MessageBoxWin32Ex(GetLastError(), hWnd);
               SetWindowLongPtr(hWnd, DWLP_MSGRESULT, -1);
               return TRUE;
           }
           return FALSE;
       }
       ```

    3. **Line 191 in UpdateCertificatePanel:**
       Remove: `CheckDlgButton(hWnd,IDC_03IMPORT,BST_UNCHECKED);`

    4. **Lines 85-86 in SelectFile function:**
       Remove: `EID::SetWindowTextW(GetDlgItem(hWnd,IDC_03FILENAME), szFile);`
       Remove: `CheckDlgButton(hWnd,IDC_03IMPORT,BST_CHECKED);`
       Also remove line 87 if it unchecks IDC_03USETHIS specifically for import context.

    5. **Line 338 in PSN_WIZNEXT handler:**
       Remove: `if (IsDlgButtonChecked(hWnd, IDC_03IMPORT) && HandleImportOption(hWnd)) return TRUE;`

    6. **Lines 396-398 in WM_COMMAND handler:**
       Remove the entire case:
       ```cpp
       case IDC_03SELECTFILE:
           SelectFile(hWnd);
           break;
       ```

    **Code sections to MODIFY:**

    7. **SelectFile function (lines 62-92):**
       Since this function is now only called for IDC_03SELECTFILE which is being removed, consider whether to:
       - Remove the entire SelectFile function (it has no other callers)
       - OR keep it for potential future use

       Decision: Remove the entire SelectFile function since:
       - It is only referenced by IDC_03SELECTFILE handler (being removed)
       - It references IDC_03FILENAME and IDC_03IMPORT (both being removed)
       - No other code calls this function

    **Do NOT modify:**
    - ImportFileToSmartCard declaration/reference in CertificateUtilities.h - this is a library function that may be used elsewhere
    - Resource ID macros in EIDConfigurationWizard.h - keep them for stability

    **After removal:**
    - The PSN_WIZNEXT handler will only check for IDC_03DELETE, IDC_03_CREATE, and IDC_03USETHIS
    - UpdateCertificatePanel will only uncheck IDC_03IMPORT (removed) and manage IDC_03USETHIS and IDC_03_CREATE
  </action>
  <verify>
    1. Search for "HandleImportOption" in the file - should return 0 results
    2. Search for "IDC_03IMPORT" in the file - should return 0 results
    3. Search for "IDC_03FILENAME" in the file - should return 0 results
    4. Search for "IDC_03SELECTFILE" in the file - should return 0 results
    5. Search for "IDC_03IMPORTPASSWORD" in the file - should return 0 results
    6. Search for "SelectFile" in the file - should return 0 results (function removed)
    7. Verify PSN_WIZNEXT handler only has 3 option checks: IDC_03DELETE, IDC_03_CREATE, IDC_03USETHIS
  </verify>
  <done>
    EIDConfigurationWizardPage03.cpp contains no references to P12 import functionality. The PSN_WIZNEXT handler only processes Delete, Create, and UseThis options.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build and verify compilation</name>
  <files>EIDConfigurationWizard/EIDConfigurationWizard.vcxproj</files>
  <action>
    Build the entire solution using build.ps1 and verify successful compilation with no errors.

    **Build command:**
    ```
    ./build.ps1
    ```

    **Expected results:**
    - Build succeeds with 0 errors
    - May have warnings but no new warnings related to the removed code

    **If build fails:**
    1. Check for any remaining references to removed control IDs
    2. Check for any code paths that assume IDC_03IMPORT exists
    3. Verify resource compiler successfully processes the modified .rc file

    **After successful build:**
    The EIDConfigurationWizard.exe should be smaller in size due to removed code.
  </action>
  <verify>
    1. Run `./build.ps1`
    2. Check build output for successful completion
    3. Verify no compiler errors related to removed code
  </verify>
  <done>
    Build completes successfully with 0 errors. EIDConfigurationWizard.exe is generated in the output directory.
  </done>
</task>

</tasks>

<verification>
## Phase Verification

After completing all tasks, verify:

1. **Source code verification:**
   - `HandleImportOption` function does not exist in EIDConfigurationWizardPage03.cpp
   - `SelectFile` function does not exist in EIDConfigurationWizardPage03.cpp
   - No references to IDC_03IMPORT, IDC_03FILENAME, IDC_03SELECTFILE, IDC_03IMPORTPASSWORD in EIDConfigurationWizardPage03.cpp
   - PSN_WIZNEXT handler has exactly 3 option checks (Delete, Create, UseThis)

2. **Resource file verification:**
   - IDD_03NEW dialog does not contain P12 import controls
   - Dialog still functions correctly for Create and UseThis options

3. **Build verification:**
   - All 7 projects compile successfully
   - No linker errors
   - EIDConfigurationWizard.exe is generated

4. **Functional verification (visual inspection):**
   - User cannot see P12 import option in the Configuration Wizard
   - Only Create and UseThis options are visible
</verification>

<success_criteria>
1. User cannot see P12 import controls in the Configure Smart Card wizard (controls removed from IDD_03NEW)
2. User cannot trigger P12 import functionality through any UI element (handler code removed)
3. Build compiles without errors after P12 removal (verified by successful compilation)
</success_criteria>

<output>
After completion, create `.planning/phases/51-remove-p12-import/51-01-SUMMARY.md`
</output>
