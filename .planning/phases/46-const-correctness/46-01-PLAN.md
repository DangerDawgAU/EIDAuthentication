---
phase: 46-const-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EIDCredentialProvider/common.h
  - .planning/phases/46-const-correctness/46-VERIFICATION.md
autonomous: true
requirements:
  - CONST-01
  - CONST-02
  - CONST-03

must_haves:
  truths:
    - "All compile-time constant global variables are marked const"
    - "All const-eligible global pointers are const at every level (const-correctness)"
    - "Runtime-assigned globals documented with cannot-be-const rationale (LSA pointers, DLL state)"
    - "Build passes with zero errors after const additions"
  artifacts:
    - path: "EIDCredentialProvider/common.h"
      provides: "Credential provider field descriptors and state pairs"
      contains: "CREDENTIAL_PROVIDER_FIELD_DESCRIPTOR"
    - path: ".planning/phases/46-const-correctness/46-VERIFICATION.md"
      provides: "Documentation of const-correctness changes and rationale"
  key_links:
    - from: "EIDCredentialProvider/common.h"
      to: "Windows Credential Provider API"
      via: "CREDENTIAL_PROVIDER_FIELD_DESCRIPTOR struct"
      pattern: "s_rgCredProvFieldDescriptors"
---

<objective>
Mark all const-eligible global variables as const, ensuring proper const-correctness at every pointer level, and document any globals that cannot be made const with NOSONAR rationale comments.

Purpose: Achieve const-correctness for all global variables to improve code quality and satisfy SonarQube rule "Global variables should be const" (S1259).

Output: All global variables either marked const or documented with NOSONAR rationale explaining why const cannot be applied.
</objective>

<execution_context>
@C:/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-critical-fixes/45-01-SUMMARY.md

# Phase 46 Success Criteria (from ROADMAP)
1. All compile-time constant global variables are marked const
2. All const-eligible global pointers are const at every level (const-correctness)
3. Runtime-assigned globals documented with cannot-be-const rationale (LSA pointers, DLL state)
4. Build passes with zero errors after const additions

# Key Constraints
- **LSASS context:** No exceptions, no console I/O, static CRT required
- **Windows 7 support:** Must use v143 toolset
- **C++23 flag:** Use `/std:c++23preview`
- **Suppression format:** `// NOSONAR - <CODE>: <rationale>` for items that cannot be fixed
- Rationale codes: RUNTIME-01, GLOBAL-01, HANDLE-01, RESOURCE-01, MACRO-02, SEH-01, LSASS-01

# SonarQube Issues to Address
From Phase 45 baseline (sonarqube_issues/):
- High: Global variables should be const (common.h lines 98, 111) - `s_rgCredProvFieldDescriptors` and `s_rgMessageCredProvFieldDescriptors`
- These are CREDENTIAL_PROVIDER_FIELD_DESCRIPTOR arrays that could be const but need verification

# Current State
Most runtime-assigned globals already have NOSONAR comments with RUNTIME-01 rationale:
- EIDLogManager.cpp: hFile, hInst
- EIDConfigurationWizard: dwCurrentCredential, fHasDeselected, fGotoNewScreen, dwWizardError, etc.
- EIDCardLibrary/Package.cpp: MyAllocateHeap, MyFreeHeap, MyImpersonate
- EIDCredentialProvider/Dll.cpp: g_hinst
- EIDCardLibrary/Tracing.cpp: bFirst, IsTracingEnabled
- EIDCardLibrary/StoredCredentialManagement.cpp: samsrvDll
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze and fix const-correctness in common.h global arrays</name>
  <files>EIDCredentialProvider/common.h</files>
  <action>
Analyze the two global arrays flagged by SonarQube:
1. `s_rgCredProvFieldDescriptors` (line 98)
2. `s_rgMessageCredProvFieldDescriptors` (line 111)

These arrays of CREDENTIAL_PROVIDER_FIELD_DESCRIPTOR are used by the Windows Credential Provider API.

**Decision criteria:**
- If these arrays are only read (never modified after initialization), add `const` keyword
- If they MUST be non-const due to Windows API requirements (e.g., GetFieldDescriptorAt modifies them), add/update NOSONAR comment with GLOBAL-01 rationale

**Current state analysis:**
The arrays are defined with comment "NOSONAR - GLOBAL-01: Non-const for Windows API credential provider compatibility" but SonarQube still flags them. Verify if they truly need to be non-const:
1. Search for any code that modifies these arrays
2. Check if they are passed to functions that require non-const pointers
3. If they can be const, change to `static const CREDENTIAL_PROVIDER_FIELD_DESCRIPTOR s_rgCredProvFieldDescriptors[]`
4. If they MUST remain non-const, ensure NOSONAR comment is properly formatted

**Implementation:**
- If arrays can be const: Change `static CREDENTIAL_PROVIDER_FIELD_DESCRIPTOR` to `static const CREDENTIAL_PROVIDER_FIELD_DESCRIPTOR`
- Update or remove the NOSONAR comment as appropriate
  </action>
  <verify>
1. Build passes: `powershell -File build.ps1`
2. No new compiler warnings
3. Grep confirms const keyword added or NOSONAR comment properly positioned:
   - `grep -n "s_rgCredProvFieldDescriptors" EIDCredentialProvider/common.h`
   - `grep -n "s_rgMessageCredProvFieldDescriptors" EIDCredentialProvider/common.h`
  </verify>
  <done>
- Arrays are either marked `const` OR have properly formatted NOSONAR comments
- Build passes with zero errors
- SonarQube issues for these lines would be resolved on next scan
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all runtime-assigned globals have NOSONAR documentation</name>
  <files>Multiple (verification only, no changes expected)</files>
  <action>
Verify that all runtime-assigned global variables have appropriate NOSONAR comments.

**Known runtime-assigned globals (should already have NOSONAR):**
1. EIDLogManager/EIDLogManager.cpp:
   - `HANDLE hFile = nullptr;` - file handle opened at runtime
   - `HINSTANCE hInst;` - set by Windows at WinMain

2. EIDConfigurationWizard:
   - `DWORD dwCurrentCredential = 0xFFFFFFFF;` - selected credential index
   - `BOOL fHasDeselected = TRUE;` - UI state flag
   - `BOOL fGotoNewScreen = FALSE;` - navigation flag
   - `DWORD dwWizardError = 0;` - error code
   - `HWND hwndInvalidPasswordBalloon = nullptr;` - tooltip window
   - `PCCERT_CONTEXT pRootCertificate = nullptr;` - certificate context
   - `HANDLE hInternalLogWriteHandle = nullptr;` - file handle

3. EIDCardLibrary:
   - `PLSA_ALLOCATE_LSA_HEAP MyAllocateHeap = nullptr;` - LSA function pointer
   - `PLSA_FREE_LSA_HEAP MyFreeHeap = nullptr;` - LSA function pointer
   - `PLSA_IMPERSONATE_CLIENT MyImpersonate = nullptr;` - LSA function pointer
   - `BOOL bFirst = TRUE;` - one-time init flag
   - `BOOL IsTracingEnabled = FALSE;` - ETW state
   - `HMODULE samsrvDll = nullptr;` - DLL handle

4. EIDCredentialProvider/Dll.cpp:
   - `HINSTANCE g_hinst = nullptr;` - DLL instance handle

**Action:**
For each global, verify it has a NOSONAR comment in format: `// NOSONAR - RUNTIME-01: <specific reason>`
If missing, add the comment. This is verification-only - most should already be documented from previous phases.
  </action>
  <verify>
Run grep to find any runtime globals WITHOUT NOSONAR:
```bash
grep -rn "^static\|^[A-Z]" --include="*.cpp" | grep -v "NOSONAR" | grep -v "const\|constexpr\|inline"
```
Then verify each found item is either already const or needs NOSONAR added.
  </verify>
  <done>
- All runtime-assigned globals have NOSONAR comments with RUNTIME-01 rationale
- No new undocumented non-const globals found
  </done>
</task>

<task type="auto">
  <name>Task 3: Scan for additional const-eligible globals across all source files</name>
  <files>Multiple .cpp and .h files (potential changes)</files>
  <action>
Perform comprehensive scan for any global variables that could be marked const but are not.

**Search patterns:**
1. Static globals without const:
   ```cpp
   static <type> <name> = <value>;
   ```

2. Non-static globals (extern):
   ```cpp
   <type> <name> = <value>;
   ```

**For each found global:**
1. Determine if the value is truly constant (compile-time known or only set once at startup from a constant source)
2. If constant: Add `const` qualifier
3. If modified at runtime: Add NOSONAR comment with appropriate rationale code

**Categories to check:**
- String literals used as globals
- Numeric constants
- GUID definitions
- Handle constants (if truly constant like INVALID_HANDLE_VALUE)
- Buffer sizes and array dimensions

**Avoid changing:**
- Variables used in SEH blocks (may have special requirements)
- COM interface pointers
- Variables passed to Windows APIs that require non-const
  </action>
  <verify>
1. Build passes: `powershell -File build.ps1`
2. Count of const-eligible globals without const should be zero:
   ```bash
   grep -rn "^static [A-Za-z]" --include="*.cpp" | grep -v "const\|constexpr\|NOSONAR" | wc -l
   ```
  </verify>
  <done>
- All const-eligible globals identified and either marked const or documented with NOSONAR
- Build passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 4: Build verification and documentation</name>
  <files>.planning/phases/46-const-correctness/46-VERIFICATION.md</files>
  <action>
After all const changes, verify the build and create documentation.

**Build verification:**
1. Run full build: `powershell -File build.ps1`
2. Verify zero errors
3. Check for new warnings (should be none)
4. Verify all 7 DLLs/EXEs compile successfully

**Create VERIFICATION.md documenting:**
1. List of globals changed from non-const to const
2. List of globals that remain non-const with their NOSONAR rationale codes
3. Build confirmation (0 errors, artifacts exist)
4. Summary of changes by file

**Documentation format:**
```markdown
## CONST-01: Global Variables Made Const
| File | Variable | Change |
|------|----------|--------|
| common.h | s_rgCredProvFieldDescriptors | Added const |

## CONST-02: Pointer Const-Correctness
(If any changes)

## CONST-03: Runtime-Assigned Globals (Cannot Be Const)
| File | Variable | Rationale |
|------|----------|-----------|
| Package.cpp | MyAllocateHeap | RUNTIME-01: LSA heap allocator, set by LSA |
```
  </action>
  <verify>
1. Build passes: `powershell -File build.ps1`
2. All 7 artifacts exist in x64/Release/
3. VERIFICATION.md created with complete documentation
4. Git status shows expected file changes
  </verify>
  <done>
- Build passes with zero errors
- VERIFICATION.md documents all const-correctness decisions
- All requirements CONST-01, CONST-02, CONST-03 satisfied
  </done>
</task>

</tasks>

<verification>
## Overall Phase Verification

1. **CONST-01 Verification**: All compile-time constant globals marked const
   - Search for any remaining non-const globals that could be const
   - Each should either have `const` keyword or valid NOSONAR comment

2. **CONST-02 Verification**: Pointer const-correctness
   - Global pointers have const at every level where applicable
   - `const type* const ptr` pattern used where both levels are const

3. **CONST-03 Verification**: Runtime-assigned globals documented
   - Every non-const global has NOSONAR comment
   - Rationale codes match the reason (RUNTIME-01, GLOBAL-01, etc.)

4. **Build Verification**:
   - All 7 projects compile with 0 errors
   - No new warnings introduced
   - All artifacts present in x64/Release/

## Commands
```powershell
# Full build
powershell -File build.ps1

# Verify artifacts
dir x64\Release\*.dll, x64\Release\*.exe, x64\Release\*.lib
```
</verification>

<success_criteria>
- [ ] All compile-time constant global variables are marked const (CONST-01)
- [ ] All const-eligible global pointers are const at every level (CONST-02)
- [ ] All runtime-assigned globals documented with cannot-be-const rationale (CONST-03)
- [ ] Build passes with zero errors after const additions
- [ ] VERIFICATION.md created documenting all changes
</success_criteria>

<output>
After completion, create `.planning/phases/46-const-correctness/46-01-SUMMARY.md`
</output>
