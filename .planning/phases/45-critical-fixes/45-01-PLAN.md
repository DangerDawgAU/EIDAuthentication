---
phase: 45-critical-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [CRIT-01, CRIT-02]

must_haves:
  truths:
    - "Fall-through annotation [[fallthrough]] is present at line 44 of EIDConfigurationWizardPage06.cpp"
    - "Fall-through is correctly placed between NM_CLICK/NM_RETURN cases and PSN_SETACTIVE case"
    - "All 7 projects compile with zero errors"
    - "No new compiler warnings introduced compared to baseline"
  artifacts:
    - path: "EIDConfigurationWizard/EIDConfigurationWizardPage06.cpp"
      provides: "Switch statement with annotated fall-through"
      contains: "[[fallthrough]]"
    - path: "x64/Release/EIDAuthenticationPackage.dll"
      provides: "Compiled authentication package"
    - path: "x64/Release/EIDCredentialProvider.dll"
      provides: "Compiled credential provider"
    - path: "x64/Release/EIDCardLibrary.dll"
      provides: "Compiled smart card library"
    - path: "x64/Release/EIDConfigurationWizard.exe"
      provides: "Compiled configuration wizard"
    - path: "x64/Release/EIDConfigurationWizardElevated.exe"
      provides: "Compiled elevated helper"
    - path: "x64/Release/EIDLogManager.exe"
      provides: "Compiled log manager"
    - path: "x64/Release/EIDPasswordChangeNotification.dll"
      provides: "Compiled password change notification"
  key_links:
    - from: "EIDConfigurationWizardPage06.cpp"
      to: "switch statement at line 18"
      via: "case NM_CLICK/NM_RETURN falling through to PSN_SETACTIVE"
      pattern: "\\[\\[fallthrough\\]\\]"
---

<objective>
Verify that the blocker fall-through fix from v1.2 is present and correct, and confirm all 7 projects build cleanly with zero errors.

Purpose: Ensure the codebase is in a stable state before proceeding with v1.6 SonarQube remediation work.
Output: Verification report confirming CRIT-01 fix is present and CRIT-02 build passes.
</objective>

<execution_context>
@C:/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md

## Phase Context

**Phase 45: Critical Fixes**
- Goal: Verify blocker fix from v1.2 is in place and all projects build cleanly
- Requirements: CRIT-01 (verify fall-through annotation), CRIT-02 (verify clean build)
- No dependencies on other phases (foundation phase for v1.6)

## Prior Work Reference

Phase 15 (v1.2) fixed the blocker issue:
- File: `EIDConfigurationWizard/EIDConfigurationWizardPage06.cpp`
- Line: 44
- Fix: Added `[[fallthrough]]` annotation to unannotated fall-through between switch labels

The switch statement handles WM_NOTIFY messages where NM_CLICK/NM_RETURN intentionally fall through to PSN_SETACTIVE handling.

## Build Context

- Solution: `EIDCredentialProvider.sln`
- Configuration: Release
- Platform: x64
- Toolset: v143 (Windows 7+ support)
- C++ Standard: /std:c++23preview

The CI workflow (`.github/workflows/scan-artifacts.yml`) confirms the build command:
```
msbuild EIDCredentialProvider.sln /p:Configuration=Release /p:Platform=x64 /m /v:minimal
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify fall-through annotation (CRIT-01)</name>
  <files>EIDConfigurationWizard/EIDConfigurationWizardPage06.cpp</files>
  <action>
    Verify that the `[[fallthrough]]` attribute is present at line 44 of `EIDConfigurationWizard/EIDConfigurationWizardPage06.cpp`.

    The expected code structure in the WM_NOTIFY handler (starting around line 34) should be:
    ```cpp
    case NM_CLICK:
    case NM_RETURN:
        {
            // enable / disable policy
        }
        [[fallthrough]];  // This MUST be present at line 44
    case PSN_SETACTIVE:
        // ... handler code
    ```

    Verification steps:
    1. Read the file and locate line 44
    2. Confirm `[[fallthrough]]` is present
    3. Confirm it is positioned correctly between the closing brace of NM_CLICK/NM_RETURN and the PSN_SETACTIVE case
    4. Confirm the syntax is correct (double brackets, semicolon after)

    If the annotation is missing or incorrect, this is a CRITICAL issue that must be reported immediately.
  </action>
  <verify>
    Grep for `[[fallthrough]]` in the file:
    ```
    grep -n "\[\[fallthrough\]\]" EIDConfigurationWizard/EIDConfigurationWizardPage06.cpp
    ```
    Expected output: `44:				[[fallthrough]];`
  </verify>
  <done>
    Fall-through annotation verified present at line 44 with correct syntax and placement.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build all projects and verify zero errors (CRIT-02)</name>
  <files></files>
  <action>
    Build the entire solution to verify all 7 projects compile successfully.

    Build command:
    ```
    msbuild EIDCredentialProvider.sln /p:Configuration=Release /p:Platform=x64 /m /v:minimal
    ```

    Projects to verify (7 total):
    1. EIDAuthenticationPackage (DLL - LSASS integration)
    2. EIDCredentialProvider (DLL - Windows login UI)
    3. EIDCardLibrary (DLL - shared smart card utilities)
    4. EIDConfigurationWizard (EXE - user configuration tool)
    5. EIDConfigurationWizardElevated (EXE - UAC elevated helper)
    6. EIDLogManager (EXE - ETW tracing control)
    7. EIDPasswordChangeNotification (DLL - password filter)

    Expected outputs in x64/Release/:
    - EIDAuthenticationPackage.dll
    - EIDCredentialProvider.dll
    - EIDCardLibrary.dll
    - EIDConfigurationWizard.exe
    - EIDConfigurationWizardElevated.exe
    - EIDLogManager.exe
    - EIDPasswordChangeNotification.dll

    Key constraints to maintain:
    - No exceptions (LSASS context)
    - Static CRT (/MT) for LSASS isolation
    - v143 toolset for Windows 7+ support
    - /std:c++23preview for C++23 features

    Count and report:
    - Total errors (MUST be 0)
    - Total warnings (note count, flag if significantly higher than expected)
  </action>
  <verify>
    1. Run msbuild and capture output
    2. Check build result: `echo $?` should be 0 (success)
    3. Verify all 7 output files exist:
       ```
       ls -la x64/Release/*.dll x64/Release/*.exe
       ```
    4. Parse build output for "error" count (should be 0)
  </verify>
  <done>
    - All 7 projects compiled with 0 errors
    - All 7 output files exist in x64/Release/
    - Warning count noted (no significant increase from baseline)
  </done>
</task>

<task type="auto">
  <name>Task 3: Document verification results</name>
  <files>.planning/phases/45-critical-fixes/45-VERIFICATION.md</files>
  <action>
    Create a verification report documenting the results of CRIT-01 and CRIT-02 verification.

    The report should include:
    1. **CRIT-01 Verification**: Confirmation that `[[fallthrough]]` is present at line 44 with code snippet showing context
    2. **CRIT-02 Verification**: Build results including:
       - Build command used
       - Build status (success/failure)
       - Error count (must be 0)
       - Warning count
       - List of compiled artifacts with file sizes
    3. **Summary**: Pass/fail status for each requirement
    4. **Timestamp**: Date/time of verification

    Format the report as markdown suitable for inclusion in project documentation.
  </action>
  <verify>
    Verify the VERIFICATION.md file exists and contains:
    - CRIT-01 status
    - CRIT-02 status
    - Build output summary
  </verify>
  <done>
    Verification report created at `.planning/phases/45-critical-fixes/45-VERIFICATION.md` with complete results for CRIT-01 and CRIT-02.
  </done>
</task>

</tasks>

<verification>
1. Confirm `[[fallthrough]]` annotation is present at line 44 of EIDConfigurationWizardPage06.cpp
2. Confirm build completes with 0 errors
3. Confirm all 7 compiled artifacts exist in x64/Release/
4. Confirm verification report is created
</verification>

<success_criteria>
1. Fall-through annotation verified present and correct at line 44 (CRIT-01 PASS)
2. All 7 projects compile with zero errors (CRIT-02 PASS)
3. Verification report documents both requirements as verified
</success_criteria>

<output>
After completion, create `.planning/phases/45-critical-fixes/45-01-SUMMARY.md`
</output>
