---
phase: 47-control-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EIDCardLibrary/CertificateUtilities.cpp
  - EIDCardLibrary/TraceExport.cpp
  - .planning/phases/47-control-flow/47-VERIFICATION.md
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03

must_haves:
  truths:
    - "Nesting depth reduced to <= 3 levels where safe via guard clauses and early returns"
    - "Redundant if statements merged with enclosing conditionals"
    - "SEH/COM blocks that cannot be restructured are documented with rationale"
    - "Build passes with zero errors after control flow changes"
  artifacts:
    - path: "EIDCardLibrary/CertificateUtilities.cpp"
      provides: "Empty compound statements filled with NOSONAR comments"
      contains: "NOSONAR"
    - path: "EIDCardLibrary/TraceExport.cpp"
      provides: "Empty compound statement documented"
      contains: "NOSONAR"
    - path: ".planning/phases/47-control-flow/47-VERIFICATION.md"
      provides: "Documentation of control flow changes"
  key_links:
    - from: "CertificateUtilities.cpp"
      to: "SEH __try/__except blocks"
      via: "Empty catch blocks for expected failures"
      pattern: "__except|__try"
---

<objective>
Reduce control flow complexity by filling empty compound statements with NOSONAR comments, documenting SEH blocks that cannot be restructured, and verifying all remaining nesting issues have appropriate documentation.

Purpose: Satisfy SonarQube control flow rules (S108 empty blocks) while preserving LSASS safety by not restructuring SEH-protected code.

Output: Empty compound statements documented with rationale, SEH blocks documented, VERIFICATION.md created.
</objective>

<execution_context>
@C:/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-nesting-reduction/37-01-SUMMARY.md

# Phase 47 Success Criteria (from ROADMAP)
1. Nesting depth reduced to <= 3 levels where safe via guard clauses and early returns
2. Redundant if statements merged with enclosing conditionals
3. SEH/COM blocks that cannot be restructured are documented with rationale
4. Build passes with zero errors after control flow changes

# Requirements
- FLOW-01: Reduce nesting depth to <= 3 levels via guard clauses and early returns
- FLOW-02: Merge redundant if statements with enclosing conditionals
- FLOW-03: SEH/COM blocks that cannot be restructured documented with rationale

# Key Constraints
- **LSASS context:** No exceptions, no console I/O, static CRT required
- **SEH blocks:** `__try/__finally` patterns must be preserved for structured exception handling
- **Windows 7 support:** Must use v143 toolset
- **Suppression format:** `// NOSONAR - <CODE>: <rationale>`
- Rationale codes: SEH-01 for SEH-related, EMPTY-01 for intentionally empty blocks

# Prior Work (Phase 37)
Phase 37 already:
- Added guard clauses to CEIDProvider.cpp and CEIDCredential.cpp
- Documented 21 SEH-protected functions in StoredCredentialManagement.cpp with won't-fix comments
- Reduced nesting in GetSerialization from 5+ levels to sequential guard clauses

# Current SonarQube Issues to Address
From sonarqube_issues/ baseline:
1. **S108 Empty compound statements (Medium)**:
   - CertificateUtilities.cpp:932 - Empty if block in SEH context
   - CertificateUtilities.cpp:1033 - Empty if block in SEH context
   - CertificateUtilities.cpp:1052 - Empty if block in SEH context
   - TraceExport.cpp:68 - Empty block

2. **S134 Nesting depth (already addressed in Phase 37)**:
   - Most nesting issues resolved via guard clauses
   - Remaining issues in SEH blocks documented as won't-fix

3. **Cognitive Complexity (High)** - deferred to Phase 48/49:
   - EIDConfigurationWizardPage03.cpp:285 - Complexity 44
   - CContainerHolder.cpp:264 - Complexity 37
   - These require helper extraction which is more extensive

# Analysis of Empty Blocks
The empty blocks in CertificateUtilities.cpp appear in SEH context:
```cpp
if (CertAddCertificateContextToStore(hCertStore,pNewCertificateContext,CERT_STORE_ADD_ALWAYS,nullptr))
{
    // Empty success block - continue to next operation
}
else
{
    dwError = GetLastError();
    __leave;
}
```

This is an intentional pattern where success means "do nothing, continue" and only failure requires handling.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NOSONAR comments to empty compound statements in CertificateUtilities.cpp</name>
  <files>EIDCardLibrary/CertificateUtilities.cpp</files>
  <action>
Add NOSONAR comments to the empty compound statements identified by SonarQube S108.

**Files and lines to modify:**
1. Line ~932: Empty success block after CertAddCertificateContextToStore
2. Line ~1033: Empty success block (similar pattern)
3. Line ~1052: Empty success block (similar pattern)

**Comment format:**
```cpp
if (CertAddCertificateContextToStore(...))
{
    // NOSONAR - EMPTY-01: Intentionally empty - success means continue without action
}
```

**Why these blocks are empty:**
These follow the pattern where the if-block handles success (continue silently) and the else-block handles failure (log error and __leave). This is intentional control flow in SEH-protected code.

**Instructions:**
1. Read CertificateUtilities.cpp to find exact line numbers
2. Add inline comments explaining why each block is intentionally empty
3. Use format: `// NOSONAR - EMPTY-01: <specific reason>`
4. Ensure comments are descriptive of why the block is empty
  </action>
  <verify>
1. Grep finds NOSONAR comments at expected locations:
   ```bash
   grep -n "NOSONAR.*EMPTY-01" EIDCardLibrary/CertificateUtilities.cpp
   ```
2. No new compiler warnings
3. Empty block SonarQube issues will be resolved on next scan
  </verify>
  <done>
- Three empty compound statements in CertificateUtilities.cpp have NOSONAR comments with EMPTY-01 rationale
- Comments explain why each block is intentionally empty
- Code compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add NOSONAR comment to empty compound statement in TraceExport.cpp</name>
  <files>EIDCardLibrary/TraceExport.cpp</files>
  <action>
Add NOSONAR comment to the empty compound statement at line ~68 in TraceExport.cpp.

**Read the file first to understand context:**
The empty block may be in an SEH context or error handling path.

**Comment format:**
```cpp
{
    // NOSONAR - EMPTY-01: <specific reason based on context>
}
```

**Instructions:**
1. Read TraceExport.cpp around line 68 to understand the context
2. Determine why the block is empty (SEH, success continuation, placeholder, etc.)
3. Add appropriate NOSONAR comment with EMPTY-01 code
4. If the block can be removed entirely (dead code), remove it instead
  </action>
  <verify>
1. Grep finds NOSONAR comment or confirms empty block removed:
   ```bash
   grep -n "NOSONAR" EIDCardLibrary/TraceExport.cpp
   ```
2. Code compiles without errors
  </verify>
  <done>
- Empty compound statement in TraceExport.cpp documented with NOSONAR or removed
- Code compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify SEH block documentation from Phase 37</name>
  <files>EIDCardLibrary/StoredCredentialManagement.cpp</files>
  <action>
Verify that the 21 SEH-protected functions documented in Phase 37 still have their NOSONAR comments intact.

**From Phase 37 SUMMARY:**
- 21 SEH-protected functions in StoredCredentialManagement.cpp have won't-fix comments
- Format: `// SonarQube S134: Won't Fix - SEH-protected function (__try/__finally)`

**Verification steps:**
1. Count SEH won't-fix comments in the file
2. Verify count matches expected (21)
3. Check that each __try block has associated documentation
4. Confirm no new SEH blocks were added without documentation

**If discrepancies found:**
- Add missing NOSONAR comments using format: `// NOSONAR - SEH-01: SEH-protected function (__try/__finally) - cannot extract code from protected block`
  </action>
  <verify>
1. Count of SEH-related NOSONAR comments matches or exceeds expected:
   ```bash
   grep -c "SEH-01\|SEH.*Won't Fix\|__try.*NOSONAR" EIDCardLibrary/StoredCredentialManagement.cpp
   ```
2. All __try blocks have associated documentation
  </verify>
  <done>
- All SEH blocks documented with NOSONAR rationale
- FLOW-03 requirement satisfied (SEH/COM blocks documented)
  </done>
</task>

<task type="auto">
  <name>Task 4: Build verification and create VERIFICATION.md</name>
  <files>.planning/phases/47-control-flow/47-VERIFICATION.md</files>
  <action>
After all control flow changes, verify the build and create documentation.

**Build verification:**
1. Run full build: `powershell -File build.ps1`
2. Verify zero errors
3. Check for new warnings (should be none)
4. Verify all 7 DLLs/EXEs compile successfully

**Create VERIFICATION.md documenting:**
1. List of empty blocks filled with NOSONAR comments
2. List of SEH blocks documented with rationale
3. Build confirmation (0 errors, artifacts exist)
4. Summary of changes by file

**Documentation format:**
```markdown
## FLOW-01: Nesting Depth
(Already addressed in Phase 37 - guard clauses in CEIDProvider.cpp, CEIDCredential.cpp)

## FLOW-02: Redundant Conditionals
(Any merged conditionals listed here)

## FLOW-03: SEH/COM Block Documentation
| File | Line | Pattern | Rationale |
|------|------|---------|-----------|
| CertificateUtilities.cpp | 932 | Empty success block | EMPTY-01: Success continues silently |
| TraceExport.cpp | 68 | Empty block | EMPTY-01: <reason> |
| StoredCredentialManagement.cpp | Multiple | __try/__finally | SEH-01: 21 functions documented |
```
  </action>
  <verify>
1. Build passes: `powershell -File build.ps1`
2. All 7 artifacts exist in x64/Release/
3. VERIFICATION.md created with complete documentation
4. Git status shows expected file changes
  </verify>
  <done>
- Build passes with zero errors
- VERIFICATION.md documents all control flow decisions
- All requirements FLOW-01, FLOW-02, FLOW-03 satisfied
  </done>
</task>

</tasks>

<verification>
## Overall Phase Verification

1. **FLOW-01 Verification**: Nesting depth <= 3 levels where safe
   - Phase 37 already addressed this with guard clauses
   - Verify no new deeply nested code introduced
   - SEH blocks documented as exceptions

2. **FLOW-02 Verification**: Redundant conditionals merged
   - Scan for patterns like `if (a) { if (b) ... }` that could be `if (a && b)`
   - Check for redundant else-if chains

3. **FLOW-03 Verification**: SEH/COM blocks documented
   - All __try/__except/__finally blocks have NOSONAR comments
   - Rationale codes match the reason (SEH-01, EMPTY-01)

4. **Build Verification**:
   - All 7 projects compile with 0 errors
   - No new warnings introduced
   - All artifacts present in x64/Release/

## Commands
```powershell
# Full build
powershell -File build.ps1

# Verify artifacts
dir x64\Release\*.dll, x64\Release\*.exe, x64\Release\*.lib

# Count SEH documentation
grep -c "SEH" EIDCardLibrary/StoredCredentialManagement.cpp

# Count empty block fixes
grep -c "EMPTY-01" EIDCardLibrary/*.cpp
```
</verification>

<success_criteria>
- [ ] Empty compound statements filled with NOSONAR comments with EMPTY-01 rationale (FLOW-02)
- [ ] SEH/COM blocks documented with SEH-01 rationale (FLOW-03)
- [ ] Build passes with zero errors after control flow changes
- [ ] VERIFICATION.md created documenting all control flow decisions
- [ ] Phase 37 guard clause work confirmed intact (FLOW-01)
</success_criteria>

<output>
After completion, create `.planning/phases/47-control-flow/47-01-SUMMARY.md`
</output>
